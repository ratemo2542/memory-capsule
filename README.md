# Memory Capsule

A small Express-powered web app for creating a personal capsule with a recipient, occasion, message, and optional photos. Capsules are stored locally in the browser, so no data is uploaded to a server.

## Run it

```bash
npm install
npm start
```

Open `http://localhost:3000` in a browser. Use `npm test` to run JavaScript syntax checks.
